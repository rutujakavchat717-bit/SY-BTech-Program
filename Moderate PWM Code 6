#include <stdio.h>
void print_pwm_line(int duty, int period) 
{
    for(int i = 0; i < period; i++)
        printf(i < duty ? "1" : "0");
    printf("\n");
}
void main() 
{
    print_pwm_line(3, 10);
    print_pwm_line(5, 10);
    print_pwm_line(7, 10);
}
