#include <stdio.h>
void main() 
{
    int duty_cycle, period;
    printf("Enter duty & period (PWM 13): "); 
    scanf("%d %d", &duty_cycle, &period);
    printf("PWM 13: ");
    for(int i = 0; i < period; i++)
        printf(i < duty_cycle ? "1" : "0");
    printf("\n");
}
