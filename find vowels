#include <stdio.h>
#include <string.h>
int main() 
{
    char s[50];
    int i,count=0;
    gets(s);
    for(i=0;s[i];i++)
        if(strchr("aeiouAEIOU",s[i])) count++;
    printf("Vowels=%d",count);
    return 0;
}
