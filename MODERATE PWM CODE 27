#include <stdio.h>
void main() 
{
    int duty = 5, period = 10;
    printf("PWM 8 (reverse duty): ");
    for (int i = 0; i < period; i++)
        printf(i < (period - duty) ? "0" : "1");
    printf("\n");
}
