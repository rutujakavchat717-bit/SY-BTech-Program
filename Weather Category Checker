#include<stdio.h>
void main() 
{
    float temp;
    printf("Enter outside temperature (°C): ");
    scanf("%f", &temp);

    if(temp < 10)
        printf("Weather: Cold");
    else if(temp >= 10 && temp <= 25)
        printf("Weather: Pleasant");
    else
        printf("Weather: Hot");
}
