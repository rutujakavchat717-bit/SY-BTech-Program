#include <stdio.h>
void main() 
{
    int duty = 2, period = 8;
    printf("PWM 15 (Pulse in middle): ");
    for (int i = 0; i < period; i++)
        printf(i >= 3 && i < 3 + duty ? "1" : "0");
    printf("\n");
}
