#include <stdio.h>
void main() 
{
    int duty = 5, period = 10;
    printf("PWM 14 (Half pulse): ");
    for (int i = 0; i < period / 2; i++)
        printf(i < duty ? "1" : "0");
    printf("\n");
}
