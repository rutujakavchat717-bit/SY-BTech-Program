#include <stdio.h>
void main() 
{
    int duty_cycle, period;
    printf("Enter duty & period (PWM 17): "); 
    scanf("%d %d", &duty_cycle, &period);
    printf("PWM 17: ");
    for(int i = 0; i < period; i++)
        printf(i < duty_cycle ? "*" : "-");
    printf("\n");
}
