#include <stdio.h>
float totalProfit(float p[], int n) 
{
    float sum = 0;
    for(int i = 0; i < n; i++)
        sum += p[i];
    return sum;
}
int main() 
{
    float p[5];
    printf("Enter profit of 5 products: ");
    for(int i = 0; i < 5; i++)
        scanf("%f", &p[i]);
    printf("Total Profit = %.2f\n", totalProfit(p, 5));
    return 0;
}
