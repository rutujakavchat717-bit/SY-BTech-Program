#include <stdio.h>
void main() 
{
    int duty, period;
    printf("Enter duty: "); scanf("%d", &duty);
    printf("Enter period: "); scanf("%d", &period);
    printf("PWM 17: ");
    for(int i = 0; i < period; i++)
        printf(i < duty ? "@" : ".");
    printf("\n");
}
