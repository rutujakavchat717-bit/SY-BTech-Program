#include <stdio.h>
void main() 
{
    int duty = 4, period = 8;
    printf("PWM 11 in columns:\n");
    for(int i = 0; i < period; i++) 
{
        printf("Cycle %d: %c\n", i + 1, (i < duty) ? '1' : '0');
}
}
