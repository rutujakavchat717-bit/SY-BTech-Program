#include <stdio.h>
void main() 
{
    int duty, period;
    printf("Enter duty and period: ");
    scanf("%d %d", &duty, &period);
    for (int i = 0; i < period; i++)
        printf(i < duty ? "1" : "0");
    printf("\n");
}
