#include <stdio.h>
#include <conio.h>
void main()
{
    FILE *fp;
    float sensor[20];
    int n, i;

    printf("Enter number of oil pressure readings: ");
    scanf("%d", &n);

    fp = fopen("oil_pressure_log.txt", "w");
    if(fp == NULL)
    {
        printf("Error opening file!");
        getch();
        return;
    }

    fprintf(fp, "Engine Oil Pressure Log\n");
    fprintf(fp, "------------------------\n");

    for(i=0; i<n; i++)
    {
        printf("Enter pressure %d: ", i+1);
        scanf("%f", &sensor[i]);
        fprintf(fp, "Pressure %d = %.2f PSI\n", i+1, sensor[i]);
    }

    fclose(fp);

    printf("Oil pressure saved to 'oil_pressure_log.txt'.\n");
}
