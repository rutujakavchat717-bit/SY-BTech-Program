#include <stdio.h>
void main() 
{
    int duty = 60, period = 100;
    printf("PWM 2 (%% based): ");
    for (int i = 0; i < period; i++) 
{
        printf(i < duty ? "H" : "L");
}
    printf("\n");
}
