#include <studio.h>
void main() 
{
    int duty, period;
    printf("Enter duty & period: "); scanf("%d %d", &duty, &period);
    printf("PWM 9 Pattern:\n");
    for(int i = 0; i < period; i++) 
{
        printf(i < duty ? "|" : "_");
 }
    printf("\n");
}
