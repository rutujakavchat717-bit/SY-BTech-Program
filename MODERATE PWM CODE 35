#include <stdio.h>
void main() 
{
    int duty = 5, period = 10;
    printf("PWM 16 (Edge fade): ");
    for (int i = 0; i < period; i++) 
{
        if (i < duty) printf("↑");
        else printf("↓");
}
    printf("\n");
}
