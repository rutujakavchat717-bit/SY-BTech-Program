#include <stdio.h>
void main() 
{
    for(int duty = 2; duty <= 8; duty += 2) 
{
        int period = 10;
        printf("Duty %d%%: ", (duty * 10));
        for(int i = 0; i < period; i++)
            printf(i < duty ? "1" : "0");
        printf("\n");
 }
}
