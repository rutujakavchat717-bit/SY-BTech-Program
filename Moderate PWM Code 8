#include <stdio.h>
void main() 
{
    int period = 10;
    for(int duty = 1; duty <= 9; duty += 2) 
{
        printf("Duty %d: ", duty);
        for(int i = 0; i < period; i++)
            printf(i < duty ? "1" : "0");
        printf("\n");
    }
}
