#include <stdio.h>
void main() 
{
    int duty, period;
    printf("Enter duty & period (PWM9): ");
    scanf("%d %d", &duty, &period);
    printf("PWM 9: ");
    for (int i = 0; i < period; i++)
        printf(i < duty ? "#" : ".");
    printf("\n");
}
