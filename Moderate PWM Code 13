#include <stdio.h>
void main() 
{
    int duty, period, i = 0;
    printf("Enter duty & period: "); scanf("%d %d", &duty, &period);
    printf("PWM 13 (While Loop): ");
    while(i < period) 
{
        printf(i < duty ? "1" : "0");
        i++;
}
    printf("\n");
}
