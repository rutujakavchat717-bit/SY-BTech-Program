#include <stdio.h>
void main() 
{
    int duty, period;
    printf("Enter duty cycle (PWM13): ");
    scanf("%d", &duty);
    period = 10;
    printf("PWM 13: ");
    for (int i = 0; i < period; i++)
        printf(i < duty ? "+" : "-");
    printf("\n");
}
