#include <stdio.h>
void main() 
{
    int duty = 4, period = 10;
    char on = '*', off = '.';
    for (int i = 0; i < period; i++)
        printf(i < duty ? "%c" : "%c", on, off);
    printf("\n");
}
