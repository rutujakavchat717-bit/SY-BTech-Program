#include <stdio.h>
void main() 
{
    int duty = 70, period = 10;
    printf("PWM 5 (Blink Pattern): ");
    for (int i = 0; i < period; i++) 
{
        printf(i < duty / 10 ? "█" : ".");
}
    printf("\n");
}
