#include <stdio.h>
void main() 
{
    int duty, period;
    printf("Duty & Period: "); scanf("%d %d", &duty, &period);
    printf("PWM 20 (Formatted): ");
    for(int i = 0; i < period; i++)
        printf("[%c]", i < duty ? '1' : '0');
    printf("\n");
}
