#include<stdio.h>

void main() 
{
    int n, a = 0, b = 1, next;
    printf("Enter terms: ");
    scanf("%d", &n);
    
    printf("Fibonacci: ");
    for(int i = 0; i < n; i++) {
        printf("%d ", a);
        next = a + b;
        a = b;
        b = next;
    }
}
