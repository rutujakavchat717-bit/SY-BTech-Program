#include <stdio.h>
void main() 
{
    int duty, period;
    printf("Enter duty cycle and period: "); scanf("%d %d", &duty, &period);
    printf("PWM 5:\n");
    for(int i = 0; i < period; i++)
        printf(i < duty ? "*" : "-");
    printf("\n");
}
