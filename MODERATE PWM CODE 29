#include <stdio.h>
void main() 
{
    int duty = 4, period = 8;
    printf("PWM 10 (pattern flip): ");
    for (int i = 0; i < period; i++)
        printf(i % 3 == 0 ? "H" : "L");
    printf("\n");
}
