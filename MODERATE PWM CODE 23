#include <stdio.h>
void main() 
{
    int duty = 40, period = 20;
    printf("PWM 4 (Alternate): ");
    for (int i = 0; i < period; i++) 
{
        printf(i % 2 == 0 ? "1" : "0");
}
    printf("\n");
}
