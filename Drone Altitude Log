#include <stdio.h>
#include <conio.h>
void main()
{
    FILE *fp;
    float sensor[20];
    int n, i;

    printf("Enter number of drone altitude readings: ");
    scanf("%d", &n);

    fp = fopen("drone_altitude_log.txt", "w");
    if(fp == NULL)
    {
        printf("Error opening file!");
        getch();
        return;
    }

    fprintf(fp, "Drone Altitude Log\n");
    fprintf(fp, "-------------------\n");

    for(i=0; i<n; i++)
    {
        printf("Enter altitude %d: ", i+1);
        scanf("%f", &sensor[i]);
        fprintf(fp, "Altitude %d = %.2f m\n", i+1, sensor[i]);
    }

    fclose(fp);

    printf("Drone altitude saved to 'drone_altitude_log.txt'.\n");
}
