#include <stdio.h>
void main() 
{
    for(int p = 5; p <= 15; p += 5) 
{
        printf("Period %d:\n", p);
        for(int i = 0; i < p; i++)
            printf(i < p/2 ? "1" : "0");
        printf("\n");
  }
}
